const defaultSettings = {
  name: 'GroupObject',
  resizable: false,
  draggable: false,
  size: {
    width: 10,
    height: 10
  },
  has: {
    in: false,
    out: false,
    settings: true
  }
}

export default defaultSettings
const defaultSettings = {
  name: 'Organisation',
  size: {
    width: 100,
    height: 100,
    minHeight: 100,
    minWidth: 100,
    maxHeight: 1000,
    maxWidth: 1000
  },
  resizable: true,
  draggable: true,
  has: {
    in: true,
    out: true,
    settings: true
  }
}

export default defaultSettings
// import app from '../../../../main'
import EditProject from '../Project/Edit'

const moreMenuItems = [
  {
    name: 'Edit Project',
    callback: () => {
      // app.$modal.show(EditProject, {}, { scrollable: true, height: 'auto' })
    }
  },
  {
    name: 'Delete Project',
    callback: () => {

    }
  }
]

export default moreMenuItems